# Test Environment Configuration
# This file is used during automated testing
# DO NOT use production credentials here

# Server Configuration
NODE_ENV=test
PORT=5001

# Test Database (isolated from production)
MONGO_URI=mongodb://localhost:27017/evalio_test
TEST_MONGODB_URI=mongodb://localhost:27017/evalio_test

# CORS (allow test client)
CORS_ORIGIN=http://localhost:3001

# Mock API Keys (not real)
OPENAI_KEY=test_openai_key_not_real
OPENAI_MODEL=gpt-4o-mini
GEMINI_API_KEY=test_gemini_key_not_real
GEMINI_MODEL=gemini-1.5-pro
AI_PROVIDER=none

# Mock Cloudinary (no real uploads in tests)
CLOUDINARY_URL=cloudinary://test:test@test

# JWT Secret (test-only)
JWT_SECRET=test_jwt_secret_key_for_testing_only

# OCR Configuration
OCR_LANGUAGE_HINTS=en
GOOGLE_APPLICATION_CREDENTIALS=

# Redis Configuration (test instance or mock)
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_PASSWORD=
REDIS_DB=1

# Test-specific flags
SUPPRESS_TEST_LOGS=false
